# Virtual Clusters UI — Helm extension catalog

This branch is the Rancher **Extensions** Helm repository for the import-job image fix.

Add this Git repo / `gh-pages` branch as an Extension Repository in Rancher, then install **Virtual Clusters** version `1.1.1-importfix.1`.

See the [`main`](https://github.com/aeltai/virtual-clusters-importfix) branch README for full instructions.
